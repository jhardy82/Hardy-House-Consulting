export const PHI   = (1 + Math.sqrt(5)) / 2;  // Golden ratio -- the only definition
export const SQ3H  = Math.sqrt(3) / 2;          // sqrt(3)/2 -- used in hexagonal grid math
export const FOL_R = 1.0;                        // Flower of Life circle radius
